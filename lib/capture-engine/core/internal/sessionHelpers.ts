/**
 * Core-internal session helpers. Wraps external sessionMode import
 * so core only imports from internal/*. Prepares for future decoupling.
 */
export { logSession } from "@/components/CaptureWidget/session/sessionMode";
